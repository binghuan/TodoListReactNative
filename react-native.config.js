/**
 * React Native Configuration for CLI builds
 * Minimal config to fix autolinking issues
 */
module.exports = {
  project: {
    android: {
      packageName: 'com.todolistReactNative',
    },
  },
};
